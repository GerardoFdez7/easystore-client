import { Button } from '@shadcn/ui/button';

export default function ButtonSave() {
  return (
    <Button type="submit" className="bg-title hover:bg-title/80">
      Save Changes
    </Button>
  );
}
